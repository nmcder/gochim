# 변경 기록

이 파일은 사람이 읽으라고 있습니다. 무엇이 바뀌었는지보다 **왜 바뀌었는지**를 적습니다.

## 아직 배포하지 않음

### 어미를 목록이 아니라 품사로 잡습니다

못 잡는 것의 대부분은 규칙이 없어서가 아니라 **있는 규칙이 손 목록에 갇혀서**였습니다.
어미·조사는 **닫힌 집합**이라, 분석기가 그 자리를 어미로 읽었는지만 물으면 목록이 사라집니다.

**`-구요` → `-고요`.** 1층은 두 갈래로 좁혀 막고 있었습니다 — 앞 음절이 ㅆ받침일 때만,
그리고 어간 다섯 개를 손으로 적어(`하구요·되구요·있구요·없구요·좋구요`). 좁힐 수밖에
없었던 이유는 `친구요·도구요·가구요·대구요`처럼 **`구`가 명사의 끝 음절**인 자리가 오히려
더 많기 때문입니다. 품사로는 한 줄입니다 — `구/EC` 뒤에 `요/JX`. 반례 넷이 함께 빠집니다.

**`-이여서` → `-이어서`.** 겉모양이 같은 두 가지가 있습니다.

    학생이여도 → 학생 + 이(조사) + 여도   ← 틀렸다
    종이여서   → 종이 + (이) + 여서       ← 옳다. 조사가 줄어든 것이다

뒤엣것의 `이`는 **명사의 끝 음절**이고 조사는 줄어들어 보이지 않습니다. 그래서 겉에서
`이여`의 앞 글자를 보면 안 됩니다 — `종`에도 받침이 있어 맞는 글을 고칩니다.
봐야 할 것은 **앞 형태소 자신의 끝 음절**입니다.

**`-드-` → `-더-`**도 같은 꼴입니다. 어미로 읽힌 `드…`는 표준 어미 목록에 없으므로
언제나 `더…`가 맞고, `드라마·드럼·드리다·드물다`는 애초에 어미가 아니라 조건에 안 들어옵니다.

**그리고 `-을려고` 가지가 오래 죽어 있었습니다.** `을`을 이미 떼어 낸 뒤에 그 글자를
다시 찾는 조건이라 언제나 빠져나갔습니다. `입을려고·않을려고·읽을려고·앉을려고·웃을려고·
받을려고·찾을려고` — 어느 층도 잡지 못했고, 목록에 든 `먹을려고` 하나만 1층이 잡고
있었습니다. 예시가 통과하는 것만 봐서는 알 수 없는 조용한 고장이었습니다.

    -구요        2/8  →  8/8
    -이여서      0/6  →  6/6
    -드-         0/6  →  6/6
    -을려고      1/8  →  8/8

**표본 재현율은 움직이지 않았습니다** (0.955·0.786·0.985 그대로). 골든셋에도 코퍼스에도
이 오류 갈래가 없기 때문입니다. 숫자를 살리려고 표본에 넣지 않았습니다 — 규칙을 지은
손으로 표본까지 지으면 그 순간 성적표가 거짓말을 시작합니다.

### 접사 태그로 `-하다`를 붙이려다 기각했습니다

`죄송 합니다`를 붙이는 일은 1층이 이미 하는데, **어떤 명사가 `-하다`를 받는지 손으로
적어야** 했습니다(103개). 분석기의 접사 태그(XSV·XSA)가 그 목록을 대신할 줄 알았습니다.

아니었습니다. 그 태그는 우리가 묻는 것을 모릅니다.

    제출 + 하다  →  제출/NNG + 하/XSV + 다/EF      ← 맞는 말
    두부 + 하다  →  두부/NNG + 하/XSV + 다/EF      ← 그런 말은 없다

넷이 글자 하나 다르지 않게 같은 꼴로 나옵니다. 실제로 재니 깨끗해야 하는 글 2,209조각에서
**새 오탐 5건**이었습니다(`두부 한 모`→`두부한 모`, `다시 재야 한다`→`재야한다`).
통과 조건이 **0건**이라 넣지 않았습니다. 근거는 [ADR 0007](docs/decisions/0007-rejected-xsv-tag-for-hada-attachment.md)에 측정값과 함께 적었습니다.

가른 것은 **닫힌 집합이냐**입니다. 어미·조사는 닫혀 있어 태그가 곧 답이지만,
명사는 열려 있어 태그가 "명사다"까지만 말해 줍니다.

### 관문이 3층 규칙을 재고 있지 않았습니다

규칙을 넷 더했는데 **테스트 수가 3,573에서 꿈쩍도 하지 않았습니다.** 예시 자가검증이
1층만 돌고 있었기 때문입니다. 3층 규칙의 예시 57개와 반례 100개는 어느 관문에도
걸리지 않은 채였습니다. 관문을 더하고, 일부러 깨뜨려 정말 막는지 확인했습니다.

그 관문이 곧바로 하나를 잡았습니다 — `두 가지뿐이다`를 `두 가지 뿐이다`로 가르고
있었습니다. 분석기가 `가지`를 의존명사가 아니라 **동사 `가지다`**로 읽는데, 규칙은
"체언 뒤가 아니면 가른다"고만 적혀 있어 어간 뒤에 의존명사가 서는 있을 수 없는 꼴이
그대로 통과했습니다. **없는 것을 나열하는 대신 있어야 하는 것을 적었습니다** —
관형사형 어미가 있을 때만 가릅니다.

### 받는 쪽에서만 드러나는 사고를 미리 겪어 봤습니다

앞선 관문은 파일이 다 있는지를 봤습니다. 그건 절반입니다. 나머지 절반은
**남이 이걸 어떻게 불러 쓰는가**인데, 그런 사고는 우리 테스트가 절대 못 잡습니다 —
받는 쪽이 없기 때문입니다. 그래서 tarball을 실제로 말아 새 폴더에 설치하고
ESM·CommonJS·타입스크립트 세 갈래로 불러 봤습니다.

**CommonJS에서 부르면 죽었습니다.** `exports`에 `import` 조건만 있어서, 파일에
닿기도 전에 해석 단계에서 끝났습니다.

    Error [ERR_PACKAGE_PATH_NOT_EXPORTED]:
      No "exports" main defined in .../@gochim/core/package.json

ESM이라는 말은 한마디도 나오지 않습니다. 게다가 타입스크립트는 `nodenext`에서도
**이걸 통과시킵니다** — 컴파일은 조용히 되고 실행할 때만 죽습니다. 실측했습니다.
`default` 조건을 두어 요즘 노드에서는 그냥 돌게 하고, 최상위 `await`가 섞이면
그 전제가 깨지므로 그것도 관문으로 막았습니다.

**`VERSION`의 공개 타입이 `'0.1.0'` 리터럴이었습니다.** 남이 쓴
`if (VERSION === '0.2.0')`가 "겹치는 값이 없다"며 컴파일 오류가 됩니다. 우리가 판을
올리는 순간 남의 코드가 깨지는데 그쪽에는 고칠 방법이 없습니다.

**`g` 플래그 없는 규칙을 넘기면 탭이 죽었습니다.** `exec`는 `g`가 없으면 언제나
0번째부터 다시 찾습니다. 멈추는 게 아니라 진단이 무한히 쌓입니다. `Rule`이 공개
타입이라 남이 손으로 지어 오는 것은 막을 수 없고 일어날 일입니다. 사본에 `g`를
붙여 쓰고, 훑는 자리가 반드시 앞으로만 가도록 못 박았습니다.

**석 달 전에 지운 규칙 세 개가 아직 tarball에 들어 있었습니다.** `dist`를 한 번도
비우지 않아, 소스에서 지운 파일의 컴파일 결과가 그대로 남아 함께 나갈 참이었습니다.
`buljeong`·`gueo`·`spacing-number` — 지금은 어느 규칙도 아닌 것들입니다.
소스맵이 없는 파일을 가리키는지 보는 관문이 이걸 잡아냈습니다. `build`가 먼저
`dist`를 비웁니다.

**소스맵이 33%를 차지하면서 아무 데도 닿지 않았습니다.** `sources`가
`../src/index.ts`를 가리키는데 `src`는 나가지 않습니다. 받는 쪽 번들러는 매번
경고를 뱉고, 우리한테는 아무 일도 일어나지 않아 영영 모릅니다. 컴파일 결과가
한국어 주석을 통째로 담고 있어 더 얻을 것이 거의 없으므로 뺐습니다.
코어 tarball이 **275 kB → 209 kB**가 됐습니다.

**세 패키지가 따로 나갈 수 없게 했습니다.** morph·store는 peer로 `^0.1.0`을 거는데
0.x에서 캐럿은 `<0.2.0`이라, 코어만 올리면 받는 쪽에서 곧바로 ERESOLVE입니다.
범위를 느슨하게 푸는 것은 답이 아닙니다 — 안 맞는 짝이 조용히 설치됩니다.
셋을 늘 함께 올리는 규율을 `guard`와 `prepublishOnly` 양쪽에서 강제합니다.

**README가 또 자기 숫자를 틀리고 있었습니다.** 규칙 96개 시절의 성적표, 실제와
다른 예시 출력(`start: 7` — 실측 6), 실제로는 거짓인 `autoFixSafe: true`,
그리고 따라 하면 **내장 규칙 232개가 통째로 꺼지는** 사용자 규칙 예제.
숫자를 손으로 고치는 대신 `npm run size`를 만들어 재게 했습니다. 확장 빌드가
1,024로, 새 스크립트가 1,000으로 세어 같은 파일을 두고 저장소가 서로 다른 숫자를
말하던 것도 맞췄습니다.

관문 열여섯 개 전부, 그리고 여기 더한 관문 열 개는 **하나씩 깨 보고 정말 막는지
확인한 뒤에** 남겼습니다. 통과만 보면 관문이 죽어 있어도 초록이 나옵니다.

### npm에 나가기 전에 공개 API가 스스로를 지키게 했습니다

한 번 나가면 되돌릴 수 없으니, 남이 잘못 쓰기 쉬운 자리를 라이브러리 쪽에서 막습니다.

**`applyFixes`가 남의 글을 잘라 냈습니다.** 진단은 검사한 순간의 자리를 들고 있는데,
그 뒤로 글이 바뀌면 그 자리는 다른 글자를 가리킵니다.

    '완전히 다른 문장입니다요.' + 다른 글의 진단  →  '완전히 다른돼요장입니다요.'

확장은 진작부터 세 군데에서 이 대조를 손수 했습니다. 라이브러리 쪽에 두면
쓰는 사람이 잊을 수가 없습니다.

**빈 패키지가 나갈 뻔했습니다.** `dist`는 `.gitignore` 대상이라 새 클론에서 그냥
publish하면 빈 채로 나갑니다. `files`에 `dist`를 적어 둔 것은 "있으면 넣어라"이지
"없으면 멈춰라"가 아닙니다. 세 패키지에 `prepublishOnly`와 `scripts/check-publish.mjs`를
두었고, dist를 치워 놓고 실제로 막히는지 확인했습니다.

**심각도 걸러내기를 더했다가 새 구멍을 만들 뻔했습니다.** 적대 감사가 잡았습니다.
경고를 **겹침 해소 전에** 거르면, 경고가 막고 있던 자리에서 더 공격적인 규칙이 드러납니다.

    '대금 지급 결재를 요청했습니다.'  (결재 = 승인, 맞는 글)
      기본       →  confusable/결재 (경고) — 자동 적용 안 됨
      오류만     →  결재→결제 (오류) — 뜻이 바뀐다

걸러내기는 결과를 **줄이기만** 해야지 없던 진단을 드러내서는 안 됩니다.
겹침을 푸는 뒤로 옮기고 테스트로 못 박았습니다.

**무시 사전이 두 번째 패스부터 풀렸습니다.** `ignore`는 `Iterable<string>`이라
제너레이터나 `Set.values()`를 넘길 수 있는데, `fix()`는 고정점까지 되풀이하며 매번 다시
훑습니다. 예외도 경고도 없이 사용자가 "건드리지 마라"고 눌러 둔 항목이 고쳐졌습니다.

**README가 6.8배 거짓말을 하고 있었습니다.** "gzip 18.5 kB"로 적힌 것이 실측 125 kB입니다.
그 문장이 npm 첫 화면에 박힐 자리였습니다. 이번 작업이 내내 싸운 병 — **자기가 재고 싶은
수치를 적어 두는 것** — 이 성적표가 아니라 배포용 README에 남아 있었습니다.
번들 크기와 테스트 수를 실측값으로 고쳤습니다.

### 자를 바꿨습니다 — 정밀도 1.000은 자기가 지은 문장으로 잰 값이었습니다

골든셋에서 정밀도 1.000이 나옵니다. 사실입니다. 다만 그 '정상 문장 829개'는
**규칙을 다듬는 데 함께 쓴 문장**이라, 그것으로 재면 오탐이 0으로 나올 수밖에 없습니다.

규칙을 보지 않고 따로 지은 평범한 문장 25개를 넣어 보니 **9개가 망가졌습니다.**
진짜 웹페이지에 확장을 붙여 그대로 재현했습니다.

    먹지 않거나  →  먹지 안 거나       어미 목록에 '거'가 없습니다
    십 년간      →  십 연간            공백을 어두로 오인합니다
    뒤를 이었다  →  뒤를이었다          '잇다'의 활용형을 모릅니다
    일의 일부로  →  일의 일부러         가드가 명사 열다섯 개짜리 목록입니다
    내려다보니   →  내려다 보니         합성어 목록에 빠져 있습니다

이것들이 전부 오류 등급이라 **자동 고침이 묻지도 않고 바꿨습니다.**

### 자동 고침을 기본값에서 껐습니다

밑줄로 보여 주면 눌러서 넘길 수 있지만, 묻지 않고 고치면 그대로 남습니다.
게다가 자동 고침이 켜져 있으면 밑줄도 카드도 그리지 않아 **예고가 전혀 없었습니다.**

기능이 틀린 것은 아닙니다. 틀린 것은 *무엇을 자동으로 적용해도 되는지 가리는 눈*이
없다는 쪽입니다 — 지금은 심각도 하나가 '밑줄을 그을지'와 '묻지 않고 고칠지'를
겹쳐서 정합니다. 규칙마다 자동 적용 안전 여부를 따로 표시하게 되면 그때 다시 켭니다.
설정에서 손수 켜는 길은 그대로 열어 두었습니다.

함께 막은 것:

- **확장을 꺼도 입력칸에서 손을 떼면 꺼지기 전의 진단이 적용되던 길.**
  검사 쪽은 껐는지 보고 있었는데 자동 고침 쪽은 보지 않았습니다.
- 껐을 때 들고 있던 진단을 비웁니다. 그리는 것만 멈추면 부족합니다.
- 카드의 '고치기'에도 *그 자리가 아직 그 글자인가* 대조를 넣었습니다.
  늦게 온 형태소 결과가 다시 그리는 사이에 누르면 엉뚱한 자리를 자를 수 있었습니다.

### 밖에서 온 한국어를 상시 관문으로

`data/golden/wild.json` — 규칙과 무관하게 지은 정상 문장 126개입니다.
여기서 지적이 나오면 그것이 곧 오탐입니다. **문장을 고치지 않습니다. 규칙을 고칩니다.**

저장소 자기 문서와 소스 주석도 함께 훑습니다. 테스트 자료로 쓰려고 쓴 글이 아니라
그만큼 독립적입니다. 지금 29,000자에서 열네 군데가 걸리는데 전부 형태소 층 하나입니다 —
`불변식`, `정규식`, `스크롤바`처럼 한 낱말로 굳은 전문어를 가르는 자리입니다.

지금 당장 0으로 만들 수는 없으므로 `data/golden/known-false-positives.json`에
갈래별로 적어 두고, **거기 없는 갈래가 하나라도 나오면 `npm run guard`가 실패**합니다.
개수 상한이 아니라 집합인 이유는 산문이 계속 늘기 때문입니다 —
개수로 재면 문서를 한 줄 쓸 때마다 선이 흔들립니다.

그 목록은 **줄어들기만 해야 합니다.** 한 줄을 더하는 것은
"이 오탐을 앞으로도 두겠다"는 뜻이라 그만한 이유가 있어야 합니다.

### 빚을 갚았습니다 — 밖에서 온 문장에 대한 1층 오탐 0건

관문이 세어 준 오탐을 갈래마다 지웠습니다. **재현율은 한 자리도 잃지 않았습니다**
(0.955 / 0.786 / 0.985 그대로).

목록에 빠진 낱말을 채우는 것은 뒷문입니다. 정문은 **그 자리의 문법**입니다.

    않거나·않죠·않길   `않-`이 보조용언이면 반드시 본용언의 `-지` 뒤에 온다.
                       부사 `안`은 그 자리에 서지 못한다. 어미 목록이 무엇을 빠뜨리든
                       이 조건 하나로 정상 문장이 지켜진다.
    뒤를 이었다        서술격 조사 `이다`는 체언에 붙는다. **목적격 조사 뒤에는 못 온다.**
                       그러니 `을/를` 다음의 `이었다`는 언제나 `잇다`다.
    십 년간            `년`은 의존명사라 두음법칙(제10항)의 대상이 아니다.
                       수사·수 관형사는 국어에서 몇 안 되는 **닫힌 집합**이라
                       목록으로 적어도 뒷문이 되지 않는다.
    물에 만 밥         격조사가 붙은 어절 뒤에는 보조사가 오지 않는다.
    표기뿐이다         체언 뒤의 `뿐·만큼·대로`는 조사고(제41항),
                       관형사형 어미 뒤에서만 의존명사다(제42항).

`군데`는 낱낱의 곳을 세는 의존명사라 한 낱말이고, `선거·근거·철거`처럼 앞 음절에
ㄴ·ㄹ 받침을 두고 `거`로 끝나는 말은 한자어뿐입니다 — 한자어는 새로 생기지 않으니
여기서만은 목록이 정답입니다. `할거`는 일부러 뺐습니다. 넣으면 `뭐 할거?`가 죽습니다.

덤으로 오래된 버그 하나가 드러났습니다. `거` 예외 검사가 앞뒤 몇 글자만 떼어
비교하고 있어서, **낱말이 글 첫머리에 있을 때만** 맞아떨어졌습니다 —
`별거 아니야`는 막히는데 `이건 별거 아니야`는 뚫렸습니다.

### 이모지 하나에 형태소 층이 통째로 죽고 있었습니다

garu-ko는 자리를 **코드포인트**로 주는데 자바스크립트 문자열은 **UTF-16**입니다.
이모지는 UTF-16에서 두 자리(서러게이트 쌍)를 차지하므로, 옮겨 주지 않으면
그 뒤의 모든 자리가 1씩 밀립니다. 밀린 자리로 어절을 자르면 `학교끝나고`가
`" 학교끝나"`로 잘려 나와 **어느 규칙도 맞아떨어지지 않습니다.**

    이모지 없이   재현율 0.955
    🙂 하나       재현율 0.790   ← 고치기 전

무서운 것은 이것이 **틀린 자리에 밑줄을 긋는 고장이 아니라 아무 데도 안 긋는
고장**이라는 점입니다. 조용해서 아무도 눈치채지 못했습니다.
카톡·SNS가 주 사용처인데 거기서 3층이 안 돌고 있었습니다.

`token.start/end`뿐 아니라 `segment.offset`도 코드포인트였습니다. 둘 다 옮깁니다.
서러게이트가 없는 글(거의 전부입니다)에는 표를 아예 만들지 않습니다.

이제 **0.955로, 이모지가 없을 때와 완전히 같습니다.** 세 개를 붙여도 같습니다.
관문이 개수가 아니라 **같은가**를 재고, 진단 구간이 서러게이트 쌍을 끊지 않는지도
함께 봅니다. 진짜 https 페이지에 확장을 붙여 9가지를 확인했습니다.

덤으로 관문이 새 오탐 둘을 잡았습니다. 형태소 분석기가 문맥에 따라 읽기를 바꿔
같은 `주는데`를 어떤 문장에서는 어미 `-는데`로, 어떤 문장에서는
`주 + 는 + 데(의존명사)`로 읽습니다. 뒤엣것을 믿고 가르면 **연결어미가 통째로
깨집니다.** 어미는 닫힌 집합이라 목록으로 막았고, 1층의 `nnb-de-josa`와 같은
논리로 **뒤에 조사가 붙은 자리만** 가릅니다 — 어미 뒤에는 조사가 못 오니까요.
(`모으는데에`는 그대로 잡고 `주는데`는 놓아줍니다)

### 고친 글을 다시 고쳐도 그대로입니다

`fix(fix(x)) === fix(x)`. 어겨지는 방식이 둘이었고 **둘 다 실제로 있었습니다.**

**진동.** 1층이 `표기 뿐이다`를 붙이면 형태소 층이 도로 뗐습니다. 자동 고침이 켜져
있으면 사용자 화면에서 글자가 깜빡이는 것으로 나타납니다. 영원히 멈추지 않습니다.

**미수렴.** `않되요`가 `않돼요`에서 멈췄습니다. 고친 자리가 드러낸 다음 오류를
한 번만 훑고 지나친 것입니다. 여전히 틀린 글을 돌려준 셈입니다.

`fix`는 이제 더 고칠 것이 없을 때까지 되풀이합니다(상한 4회). 표본 3,786개는
전부 두 번 안에 멈춥니다. 되풀이가 진동을 숨기지 않도록 **표본 전체에 대한
멱등 테스트**를 두 층 각각 세웠습니다. 이 테스트가 빨개지면 상한을 올릴 일이 아니라
**어느 층이 옳은지 정할** 일입니다.

확장 쪽에도 빗장을 두 개 걸었습니다.

- 자동 고침이 **방금 자기가 쓴 것을 되돌리려는 진단**은 버립니다. 규칙 쪽에서
  다툼을 없애는 것이 정문이고 이것은 아직 못 찾은 다툼에 대한 대비입니다.
- `replaceRange`가 입력칸에 되쏘는 `input` 이벤트를 **사용자 타자로 세지 않습니다.**
  세고 있었던 탓에 자동 고침이 제 손길에 밀려 다음 고침을 매번 700ms씩 미뤘습니다.

### '보여줄지'와 '묻지 않고 고칠지'를 갈랐습니다

`severity`는 **"이것이 틀렸는가"라는 국어적 판정**이고,
새로 만든 `autoFixSafe`는 **"사람이 보지 않아도 이 규칙을 믿을 수 있는가"라는 공학적
판정**입니다. 둘은 다른 축인데 그동안 겹쳐 썼습니다. 그래서 국어적으로는 분명히 옳은
규칙이 자기 **가드**가 뚫리는 바람에 맞는 글을 묻지 않고 고쳐 버렸습니다.

규칙이 자동 적용 자격을 얻는 조건은 셋이고, 셋 다 기계가 잽니다.

    (가) 밖에서 온 정상 글에서 발화 0건          맞는 글을 건드린 적이 없다
    (나) 표본에서 실제 오류를 잡은 적이 있다      '조용해서 안 걸린 것'과 가른다
    (다) 반례가 하나 이상 적혀 있다               지은 사람이 오탐을 생각해 봤다

기본값은 **거짓**입니다. 새 규칙은 자격을 증명하기 전까지 밑줄만 긋습니다.

    지금까지 (severity 만 봄)   규칙 238개 · 표본 오류 508건을 자동으로 고침
    (가)만 걸었을 때            규칙 236개 · 339건
    (가)+(나)+(다)             규칙 127개 · 339건

**규칙 수를 236에서 127로 절반 넘게 줄여도 값어치는 그대로입니다.** 나머지 109개가
자동 적용에 기여하는 것이 6건뿐이었기 때문입니다. 근거 없이 켜 둘 이유가 없었습니다.

508 → 339로 줄어든 169건은 거의 전부 `morph-eojeol-split`입니다. 이 규칙 하나가 실문
재현율의 30%를 지고 있어 끌 수 없지만, 분석기가 모르는 합성어를 그럴듯하게 쪼개므로
자동 적용은 안 됩니다. **밑줄로는 그대로 남습니다.**

관문이 재는 것은 규칙 개수가 아니라 **정상 글에서 자동 적용될 뻔한 자리**입니다.
지금 0건이고, 여기에는 `known-false-positives.json`의 면죄부가 통하지 않습니다 —
그 면죄부 노릇을 하는 것이 바로 `autoFixSafe: false`이기 때문입니다.

#### 하마터면 사용자가 오류를 통째로 잃을 뻔했습니다

자동 고침이 켜져 있으면 확장이 **밑줄을 아예 안 그리고 있었습니다.**
"어차피 알아서 고쳐지는 것에 밑줄을 그으면 고칠 것도 없는 자리에 손이 간다"는 것이
그때의 이유였고, 진단을 전부 적용하던 그때는 맞는 말이었습니다.

이제는 자격을 갖춘 것만 고칩니다. 그대로 두었다면 나머지는 **고쳐지지도 보이지도**
않았을 것입니다. 자동 고침을 켠 사람에게만 형태소 층이 통째로 사라지는 셈이라,
도구가 조용해지는 게 아니라 눈이 머는 것입니다.
이제 자동 고침이 켜져 있으면 **자동으로 고쳐지지 않을 것만** 그립니다.
진짜 https 페이지에서 확인했습니다 — `안 되요`는 소리 없이 고쳐지고,
`학교끝나고`·`물이끓으면`·`좀더`는 밑줄로 남습니다.

#### 그 밖에 드러난 것

`myeongsa-sigan` 규칙이 **자기 주석이 넣지 말라고 못 박은 낱말을 담고 있었습니다.**
바로 위 주석에 "`점심시간·근무시간·대기시간`은 사전에 올라 있어 붙여 쓰는 것이 맞다.
넣으면 안 된다"고 적혀 있는데 목록에는 `대기시간`이 들어 있었습니다. 뺐습니다.

사전형 규칙은 반례를 **항목에만** 걸 수 있었습니다. 사전 전체의 성격에서 오는 오탐은
걸 자리가 없었던 셈이라 `defineLexicon`에 `counterExamples`를 열었습니다.

### 형태소 층에도 1층과 같은 창을 보냅니다

1층은 진작부터 커서 둘레 4,000자만 잘라 보고 있었는데 **3층만 글 전체를 받고
있었습니다.** 한 줄로 두 가지가 함께 잡힙니다.

    글 길이     3층 검사 시간
      4,000자        86ms
     32,000자       912ms   ← 상한이 없었습니다

창으로 자르면 글이 아무리 길어도 86ms에서 평평합니다. 그리고 커서에서 한참 떨어진
자리가 자동 고침 대상이 되던 것도 함께 사라집니다.

창으로 자르면 진단의 자리가 창 기준이 되므로 되돌려야 합니다. 그 일은
**요청 번호에 창 시작 자리를 묶어서** 합니다 — 따로 두면 답이 도착하는 사이 커서가
움직여 창이 바뀌었을 때 엉뚱한 자리에 밑줄이 그어집니다.

### 창을 씌우니 드러난 것들

적대 감사를 붙였더니 지적 26건 중 13건이 재현됐습니다. 그중 둘이 무거웠습니다.

**요청 번호가 프레임마다 1부터 다시 셉니다.** 콘텐츠 스크립트는 `all_frames: true`라
탭마다·아이프레임마다 따로 도는데 오프스크린 문서는 브라우저에 **하나뿐**입니다.
그래서 서로 다른 프레임의 요청이 같은 열쇠에 겹쳐 쓰이고, 답이 **남의 채널로**
나갑니다. 받는 쪽의 `id === latest` 검사는 번호가 같으니 그대로 통과합니다.

창을 씌우기 전에는 "남의 진단이 온다"에 그쳤지만, 이제는 그 진단이 **내 창 오프셋으로
밀려** 내 글자 위에 그어집니다. 글이 망가지진 않습니다 — 고치는 세 길이 전부 자리를
대조합니다. 하지만 밑줄과 카드에는 그 대조가 없어, 없는 오류에 밑줄이 그어지고
눌러도 아무 일이 없는 카드가 뜹니다. 오프스크린 문서가 **자기 번호를 매기게** 했습니다.

**앞 공백이 3층을 통째로 죽입니다.** `splitSentences`가 문장 앞 공백을 잘라 내면서
`offset`은 그대로 둡니다. 그 문장의 형태소가 한 칸씩 어긋나 어느 규칙도 맞아떨어지지
않습니다 — **틀린 자리에 밑줄을 긋는 게 아니라 아무 데도 안 긋습니다.**
이모지 사고와 똑같은 갈래이고, 창이 어절 경계까지 물러나면서 흔해졌습니다.

    앞에 공백 없음   →  [3,8) 학교끝나고
    앞에 " "         →  없음        ← 3층이 죽는다

함께 고친 것들:

- 창의 어절 되감기에 상한(200자)을 뒀습니다. 없으면 공백 없는 긴 글에서 창이 글 전체로
  불어나 상한을 둔 목적이 사라집니다.
- 답이 오는 사이 확장을 끄면 밑줄이 되살아나던 길을 막았습니다.
- 커서가 창 밖으로 나가면 창을 그리로 옮겨 다시 봅니다. 없으면 창 밖은 그 자리에서
  글자를 치기 전까지 비어 있습니다.
- `session.whole` 캐시가 3층이 도착하기 전 값을 굳혀 '모두 고치기'가 3층을 빠뜨리던 것.
- 관문의 산문 수집기가 코드 조각을 지운 자리에 홀로 남은 조사를 치웁니다.
  빚 목록의 '뽑아내기 잡음' 두 갈래가 함께 사라졌습니다.

### 설정이 3층에만 안 닿고 있었습니다

워커에 보내는 메시지에 무시 사전만 실려 있었습니다. **문턱과 분류가 빠져 있어
3층만 사용자 설정을 무시했습니다.** 문턱을 0.99로 올리고 분류를 `spelling`만 켜 둔
사람에게 1층은 0건을 내는데 워커는 46건을 보냈고, 그중 44건이 꺼 둔 띄어쓰기였습니다.
**설정 화면이 거짓말을 하고 있었습니다.**

콘텐츠 스크립트 → 서비스 워커 → 오프스크린 문서 → 워커, 네 구간 전부에 실어 보냅니다.
진짜 https 페이지에서 8,018자 글로 확인했습니다 — 문턱 0.99 + `spelling`만 켜면
3층 띄어쓰기 진단이 **0건**이고, 설정을 되돌리면 다시 그어집니다.

### 자격을 사전 한 덩이가 아니라 **항목마다** 매깁니다

앞 커밋이 `autoFixSafe`를 규칙 단위로 걸었는데, `lexicon`은 규칙 **하나**에
항목이 147개입니다. 플래그 하나로 147개가 통째로 자격을 얻었고, 그 안에
**문맥 가드에 기대는 항목**이 섞여 있었습니다. 실제로 네 자리가 맞는 글을
묻지 않고 망가뜨렸습니다.

    회비 일부로 간식을 샀다      →  회비 일부러…      (一部로)
    살이 찌게 마련이다           →  살이 찌개…        (찌- + -게)
    커튼 바램이 심하다           →  커튼 바람…        (바래다의 명사형)
    물건 금새로 값을 매겼다      →  물건 금세로…      (값을 뜻하는 명사)

넷 다 사전에 반례가 적혀 있었습니다. 그런데 **반례가 가드와 같은 손으로 쓰여**
가드 안쪽만 확인하고 바깥은 건드리지 못합니다. 반례를 안전의 증거로 삼았던
제 전제가 틀렸습니다.

**가드를 달았다는 것은 그 표기가 다른 뜻으로도 읽힌다는 뜻입니다.**
그래서 사람이 표시하게 두지 않고 `defineLexicon`에서 구조로 막았습니다 —
`when`을 단 항목은 사전 전체에 플래그를 걸어도 자동 적용에서 빠집니다. 잊을 수가 없습니다.

겁대기로 그 네 문장을 `wild.json`에 넣었고, 둘은 가드 자체를 구조로 고쳤습니다.

- `금새` — 부사 `금세`는 뒤 말을 꾸미므로 **반드시 공백이 따릅니다.**
  바로 한글이 붙어 있으면 그것은 조사고, 조사를 달 수 있는 것은 명사 `금새`입니다.
- `찌게` — 음식 `찌개`는 명사라 앞말과 붙어 한 낱말을 이루거나(`김치찌게`)
  조사를 달고 있습니다(`찌게를`). 둘 다 아니면 어미 `-게`입니다.
- `일부로` — 앞말이 **닫히지 않는 집합**이라 구조로 못 풀었습니다. 경고로 내렸습니다.

### 손을 뗄 때 마지막 낱말을 지킵니다

관문은 **완성문**을 재는데 자동 고침은 `focusout`에서 마지막 어절까지 고칩니다.
정상 문장 130개를 한 글자씩 쳐 나가며 재보니 그 사이 **12갈래가 깨졌습니다.**

    밥을 먹지 않  →  밥을 먹지 안     (치던 말은 '않았다')
    안간          →  안 간            (치던 말은 '안간힘')
    덜 지         →  덜지             (치던 말은 '덜 지루했다')

사용자는 낱말을 치다 말고도 손을 뗍니다 — 다른 칸을 누르거나, Tab을 치거나,
창을 옮길 때입니다. 그래서 종결부호나 공백으로 끝났을 때만 마지막 어절을 봅니다.
잃는 것은 표본 500건 중 1건입니다. 관문이 이제 그 상황을 함께 재서 되돌아가지 못합니다.

### 관문이 기계마다 다른 것을 재고 있었습니다

관문을 붙인 첫 커밋에서 **로컬은 초록인데 CI가 빨갰습니다.**
윈도 작업본은 CRLF, CI는 LF라 저장소 산문이 822줄과 919줄로 갈렸습니다.
어디서 도느냐에 따라 다른 답을 내면 그것은 관문이 아닙니다. 줄바꿈을 한 가지로 맞췄습니다.

그 덕에 CI에서만 보이던 오탐 둘이 로컬에도 드러났고, 둘 다 규칙을 고쳐 지웠습니다.

- `툴바` → `툴 바`. **의존명사 `바`는 관형사형 어미 뒤에만 섭니다.**
  체언 뒤에 나온 `바`는 외래어 합성어의 일부지 의존명사가 아닙니다.
  이 한 줄로 이미 빚으로 적어 두었던 `스크롤바` 세 갈래도 함께 사라졌습니다.
- `않는지도` → `않는 지도`. 어미 `-는지`에 보조사 `도`가 붙은 것을
  분석기가 지도(地圖) 한 낱말로 읽었습니다. 앞 글자를 떼어 어미가 되는지 보고,
  남은 꼬리가 보조사면 그 읽기를 버립니다. 꼬리를 확인하지 않으면
  `사는가게`의 `가게`까지 어미로 오해해 정탐을 잃습니다.
- 그 글을 쓰다가 관문이 셋째를 잡았습니다. **홀로 선 `ㄹ`은 조사가 아니라 받침입니다.**
  분석기가 마땅한 길을 못 찾으면 음절을 뜯어 받침을 조사로 읽습니다 —
  `빨갰습니다`를 빠(名) + 를 + 개다로 읽는 식입니다. 표본 전체에서 이런 자리를
  열아홉 군데 세었는데 **성한 것이 하나도 없었습니다.**

### CI가 그 자를 듭니다

`npm run guard`는 어기면 종료 코드 1을 내도록 만들어져 있었는데 **CI가 부르지 않았습니다.**
CI가 부르던 성적표는 종료 코드를 아예 내지 않아, 정밀도가 무너져도 초록이었습니다.
이제 CI와 릴리스 양쪽이 guard를 부릅니다.

같은 자리에서 사흘째 빨갛던 타입 오류 둘도 고쳤습니다. 워커가 버린 요청을 알리는
갈래가 늘었는데 받는 쪽이 그것을 처리하지 않아 `undefined`를 결과로 넘길 뻔했습니다.
**관문이 빨간 채로 있으면 그 관문은 아무것도 막지 못합니다.**

### 어절 안쪽 띄어쓰기를 형태소 층이 맡는다

붙여 쓴 어절을 **품사로** 가릅니다. 짝 목록으로는 닫히지 않는 집합이었습니다.

    물이끓으면 · 기름을두르고        조사 + 용언
    남은재료는 · 힘든날에는          관형사형 + 자립명사
    할수있다 · 만날거야              의존명사
    많이내리기 · 아무리봐도          부사 + 용언
    학교끝나고 · 고장나면            명사 + 용언
    늦지않고 · 보고싶다 · 앉게됐다   어미 + 보조용언

한 낱말로 굳은 말은 분석기가 통째로 한 형태소로 내놓는다(`알아보/VV`)는 성질이 가드입니다.
1층에서 블록리스트로 막아야 했던 것이 저절로 걸러집니다.

갈래마다 규칙을 따로 두었더니 한 어절에서 두 진단이 겹쳐 엔진이 하나만 남겼습니다.
`잘못나온거`가 `잘못 나온거`로 반만 고쳐지던 자리라, 어절을 한 번 훑어 자를 곳을
모두 모은 뒤 **한 진단으로** 냅니다.

### 그 밖에 새로 잡는 것

- `시킬려고 → 시키려고`. ㄹ받침 꼴이 실제 용언인지 되물어 `칼을 갈려고`를 비켜 갑니다.
- `느낌이였다 → 느낌이었다`. 앞말이 일반명사일 때만 — 사람 이름 `민준이였다`는 그대로 둡니다.
- `주문한지 30분 → 주문한 지 30분`. 앞이 용언이고 뒤에 지나온 시간이 오는 자리만 봅니다.
- `어떻하냐고 → 어떡하냐고`, `아니였나 → 아니었나`, `다른데 가자 → 다른 데 가자`
- 단위 명사 확충 — `세컵`, `삼백그램`, `두큰술`

### 고친 것

- **모두 고치기가 한 번만 훑던 문제.** `먹을껄`은 띄어쓰기와 표기가 함께 틀려
  한 번으로는 `먹을 껄`에서 멈췄습니다. 이제 세 번까지 훑습니다.
- `약속시간`을 경고에서 오류로 올렸습니다. `좀더·둘다`는 단음절 단어가 이어질 때
  붙여 쓸 수 있다는 제46항이 걸쳐 있지만 `약속시간`에는 걸칠 조항이 없습니다.

### 한자어·표기 혼동, 높임 호응

없는 낱말은 사전에 바로 넣었습니다 — `치루고→치르고`, `가르켜→가르쳐`, `쉽상→십상`,
`제작년→재작년`, `조취를→조치를`, `알맞는→알맞은`, `무릅이→무릎이`,
`알아보실런지→알아보실는지`, `나갔구요→나갔고요`.

두 낱말이 다 실재하는 것은 **근거가 되는 말이 두 어절 안에 있을 때만** 고칩니다.
`카드로 결재→결제`, `회원증을 경신→갱신`, `최고 기록을 갱신→경신`, `무게를 늘이면→늘리면`,
`국물을 조리는→졸이는`, `밥을 앉혀→안쳐`, `면이 불기→붇기`, `일체 넣지 않았다→일절`.

높임은 두 방향을 다 봅니다.

- **빠진 높임** — `어머니께서 알려 줬는데 → 주셨는데`. 어간 위치를 알면 표 없이
  `-시-`를 끼울 수 있습니다. 어려운 것은 활용이 아니라 어디까지가 그 주어의 서술어인가입니다.
- **넘치는 높임** — `환불이 가능하십니다 → 가능합니다`. 조사가 붙어야만 발화합니다.
  `주문하신 아메리카노`의 `주문`은 주어가 아니라 서술어의 일부라서요.
- **어휘 높임** — `선생님께 물어봤을 → 여쭤봤을`, `선생님 생일 → 생신`, `어르신께 주었다 → 드렸다`

### 자동 고침이 타이핑을 앞지르던 문제

**기본값을 켜짐으로 바꿨습니다.** 켜져 있으면 밑줄도 카드도 뜨지 않고 알아서 고쳐집니다.

고칠 때 커서를 돌려놓지 않던 것이 가장 큰 사고였습니다. 앞 낱말이 고쳐지는 순간
커서가 그 자리로 끌려가서, 이어 치던 글자가 문장 가운데에 박혔습니다 —
`안녕하세요 참 않됬네요 화이팅`이 `안녕하세요 참 않 화이팅됬네요`가 되는 식입니다.

세 가지를 함께 손봤습니다.

- **커서를 되돌려 놓습니다.** 고친 길이 차이만큼 밀어 원래 자리에 놓습니다.
- **한글 조합 중에는 기다립니다.** 자모를 모으는 동안 DOM을 건드리면 조합이 깨져
  치던 글자가 통째로 날아갑니다.
- **0.7초를 기다립니다.** 검사는 0.3초 뒤에 돌지만 고치는 것은 더 뜸을 들입니다.
  빨리 치는 사람은 낱말 사이에서도 손이 멈추지 않습니다.

글자를 한 자라도 친 칸에서만 움직입니다. 남이 써 둔 글을 눌렀을 뿐인데 바뀌면 놀라니까요.
규정이 양쪽을 다 허용하는 표기(`좀더`·`둘다`)도 그대로 둡니다.

언제 고치는지도 다시 잡았습니다. **낱말이 끝나는 순간**(공백·문장부호) 그 앞을 바로 고치고,
지금 치고 있는 낱말은 **입력칸에서 손을 뗄 때** 고칩니다. 다 못 친 낱말은 거의 언제나
틀린 말처럼 보이기 때문입니다 — 정상 문장 900개를 한 글자씩 쳐 나가며 세어 보니
진행 중인 어절에 진단이 붙는 자리가 24가지 나왔습니다(`후들거`→`후들 거`, `안간`→`안 간`).

손을 뗄 때 고치는 길에서는 초점을 건드리지 않습니다. 되돌리기 스택을 지키는 방법이
초점을 요구하는데, 그걸 그대로 쓰면 다음 칸으로 넘어가려던 Tab이 되돌아옵니다.

`apps/extension/test/autofix.html`이 이 사고를 그대로 재현합니다.
커서 되돌리기를 빼면 곧바로 실패하는 것까지 확인했습니다.

설정 화면은 IndexedDB를 못 열어도 뜹니다. 예전에는 그 자리에서 멈춰
켜고 끄는 스위치까지 죽었습니다.

### 형태소 층이 실제 웹페이지에서 한 번도 돌지 않았습니다

콘텐츠 스크립트는 격리된 세계에서 돌아도 **출처(origin)는 그 페이지의 것**입니다.
그래서 확장 안의 스크립트로 워커를 만들려 하면 브라우저가 막습니다.

    Failed to construct 'Worker': Script at 'chrome-extension://…/morph-worker.js'
    cannot be accessed from origin 'https://www.google.com'.

스모크 테스트 페이지는 `file://`에서 같은 출처로 워커를 만들기 때문에 이 벽에 부딪히지
않았고, 그래서 오래 눈에 띄지 않았습니다. **실제로 쓰는 동안 재현율 0.955가 아니라
0.786으로 돌고 있었습니다.**

스크립트를 받아 와 `blob:` URL로 워커를 만드는 우회가 있지만 쓰지 않았습니다.
그 워커도 페이지의 출처에서 돌아 **페이지의 CSP**를 그대로 받습니다 —
오류가 처음 보고된 곳이 하필 구글이라 그 길도 막힙니다.

대신 확장 자신의 문서(offscreen document)에서 워커를 만듭니다. 출처도 CSP도 확장의
것이라 막힐 일이 없고, 탭이 몇이든 **하나만** 뜹니다(예전에는 프레임마다 1.6MB였습니다).
구글 검색창에서 `물이끓으면 → 물이 끓으면`, `많이내리기 → 많이 내리기`가 고쳐지는 것으로
확인했습니다. 둘 다 형태소 층만 잡는 자리입니다.

같이 고친 것 둘:

- 형태소 결과는 1층보다 늦게 오는데 자동 고침이 그 자리를 보지 않고 있었습니다.
  3층만 잡는 오류가 밑줄로만 남고 영영 고쳐지지 않았습니다.
- 워커가 앞선 요청을 버릴 때 아무 말도 하지 않아, 답을 기다리던 쪽이 매달린 채
  남았습니다. 이제 버렸다는 사실을 알려 줍니다.

### 재는 법

- `npm run guard` 가 **형태소 층까지 함께** 잽니다. 확장이 분석기를 기본값으로 켜 두므로
  1층만 재면 사용자가 보는 결과를 재지 않는 것이 됩니다.
- 표본을 15편 464건으로 늘렸습니다. 사용자가 확장에 직접 넣어 보고 짚어 준 글 두 편이
  들어 있습니다. 짚어 준 것만 고치면 그 글에만 맞춘 규칙이 됩니다.
- 여러 갈래 재현율 **0.955** (1층만 0.786) · 과교정 0건
- 모두 고친 뒤 정답과 글자까지 같은 글 **5/15** (표본을 넣었을 때는 0/15)

## [0.1.0] — 2026-08-19

첫 동작 버전. 웹 데모와 크롬 확장이 모두 돌아갑니다.

### 엔진

- `@gochim/core` — 한국어 맞춤법·띄어쓰기 교정 엔진. 순수 함수 `check(text) → Diagnostic[]` 하나가 전부입니다.
  런타임 의존성 0개, gzip 18.5 kB. DOM도 크롬 API도 WASM도 모릅니다.
- 문자열 규칙 41개. 사전형 규칙은 항목이 수백 개여도 정규식 하나로 한 번만 훑습니다.
- 받침 계산으로 규정을 코드로 적습니다 — 두음법칙(`경쟁율→경쟁률`), `-이에요/-예요`, `-ㄹ게`.
- URL·이메일·코드·HTML 태그·파일 경로·짧은 인용은 검사에서 제외합니다.

### 형태소 층

- `@gochim/morph` — garu-ko 어댑터. 코어는 `Analyzer` 인터페이스만 알고 구현은 주입받습니다.
  덕분에 코어만 쓰면 18.5 kB로 끝나고, 정확도가 더 필요할 때만 1.6 MB를 얹습니다.
- 품사를 알자 1층이 예외 목록으로 겨우 막던 것들이 저절로 걸러졌습니다
  (`실수/NNG`, `날것/NNG`, `일리/NNG`, `밖에/JX` vs `밖/NNG+에/JKB`).
- 1층이 포기했던 갈래가 열렸습니다 — `먹을만큼만 → 먹을 만큼만`.

### 무시 사전

- `@gochim/store` — IndexedDB 기반. 같은 밑줄을 매번 다시 지우게 만들면 사용자는 도구를 끕니다.
- 저장이 막히면(사생활 보호 모드, Node) 메모리 모드로 조용히 내려갑니다.

### 웹 데모

- 붙여넣기 → 교정. 거울 하이라이트 레이어 + 어절 단위 문맥 카드 + 왜 틀렸는지 설명.
- 형태소 층은 토글로 켜고, 워커에서 돕니다.
- 결과 카드는 키보드로 다룰 수 있습니다.

### 크롬 확장 (MV3)

- **권한 목록에 네트워크가 없습니다.** `permissions`는 `storage` 하나뿐입니다.
- 호스트 DOM을 건드리지 않습니다 — contenteditable은 CSS Custom Highlight API,
  textarea는 겹쳐 놓은 거울 레이어. 편집기가 깨질 구조적 위험이 없습니다.
- 300ms 디바운스 + 4,000자 창으로 타이핑을 막지 않습니다.
- 교정 적용은 `execCommand('insertText')` — 되돌리기 스택을 유지하는 유일한 방법입니다.

### 정확도

골든 테스트셋(오류 문장 227 · 정상 문장 553, 그중 326개는 오탐 유도 함정)

| | 1층만 | 형태소 층 포함 |
| --- | --- | --- |
| 정밀도 | **1.000** | **1.000** |
| 재현율 | 0.939 | 0.952 |

범주는 두 차례에 걸쳐 확장했다 — 1차(되/돼, 안/않, 어휘, 의존명사, 조사·어미, 이/히)에
2차(외래어 표기법, 높임 표현, 겹말, 한자어 오용, 자소서 문어체, 구어체)를 더했다.
2차 확장 직후 기존 규칙은 새 함정 161개에서 오탐 0을 유지했다.

### 성능

| | |
| --- | --- |
| 1층 | 4,000자 1.07 ms (p95 1.59 ms) · 2만 자 8.2 ms |
| 형태소 층 | 19 ms / 1,000자 |
| 형태소 층 초기화 | 70~101 ms (한 번만) |

### 기각한 것

- **점수 기반 오타 탐지(2층)** — 정밀도가 1.000에서 0.892로 떨어졌습니다.
  임계값을 만지는 대신 분포를 재서 정상 단어와 오타가 겹친다는 것을 확인하고 기각했습니다.
  [ADR 0006](docs/decisions/0006-rejected-score-based-typo-detection.md)에 측정값이 있습니다.

### 고친 것

- 형태소 분석이 길이에 초선형이었습니다. 문장 단위로 쪼개 선형으로 만들었습니다 (2만 자 14초 → 0.38초).
- `contenteditable`에서 커서 위치를 텍스트 노드 기준으로 읽어, 긴 글에서 늘 앞부분만 검사했습니다.
