# 변경 기록

이 파일은 사람이 읽으라고 있습니다. 무엇이 바뀌었는지보다 **왜 바뀌었는지**를 적습니다.

## [0.1.0] — 2026-08-19

첫 동작 버전. 웹 데모와 크롬 확장이 모두 돌아갑니다.

### 엔진

- `@gochim/core` — 한국어 맞춤법·띄어쓰기 교정 엔진. 순수 함수 `check(text) → Diagnostic[]` 하나가 전부입니다.
  런타임 의존성 0개, gzip 18.5 kB. DOM도 크롬 API도 WASM도 모릅니다.
- 문자열 규칙 41개. 사전형 규칙은 항목이 수백 개여도 정규식 하나로 한 번만 훑습니다.
- 받침 계산으로 규정을 코드로 적습니다 — 두음법칙(`경쟁율→경쟁률`), `-이에요/-예요`, `-ㄹ게`.
- URL·이메일·코드·HTML 태그·파일 경로·짧은 인용은 검사에서 제외합니다.

### 형태소 층

- `@gochim/morph` — garu-ko 어댑터. 코어는 `Analyzer` 인터페이스만 알고 구현은 주입받습니다.
  덕분에 코어만 쓰면 18.5 kB로 끝나고, 정확도가 더 필요할 때만 1.6 MB를 얹습니다.
- 품사를 알자 1층이 예외 목록으로 겨우 막던 것들이 저절로 걸러졌습니다
  (`실수/NNG`, `날것/NNG`, `일리/NNG`, `밖에/JX` vs `밖/NNG+에/JKB`).
- 1층이 포기했던 갈래가 열렸습니다 — `먹을만큼만 → 먹을 만큼만`.

### 무시 사전

- `@gochim/store` — IndexedDB 기반. 같은 밑줄을 매번 다시 지우게 만들면 사용자는 도구를 끕니다.
- 저장이 막히면(사생활 보호 모드, Node) 메모리 모드로 조용히 내려갑니다.

### 웹 데모

- 붙여넣기 → 교정. 거울 하이라이트 레이어 + 어절 단위 문맥 카드 + 왜 틀렸는지 설명.
- 형태소 층은 토글로 켜고, 워커에서 돕니다.
- 결과 카드는 키보드로 다룰 수 있습니다.

### 크롬 확장 (MV3)

- **권한 목록에 네트워크가 없습니다.** `permissions`는 `storage` 하나뿐입니다.
- 호스트 DOM을 건드리지 않습니다 — contenteditable은 CSS Custom Highlight API,
  textarea는 겹쳐 놓은 거울 레이어. 편집기가 깨질 구조적 위험이 없습니다.
- 300ms 디바운스 + 4,000자 창으로 타이핑을 막지 않습니다.
- 교정 적용은 `execCommand('insertText')` — 되돌리기 스택을 유지하는 유일한 방법입니다.

### 정확도

골든 테스트셋(오류 문장 209 · 정상 문장 512, 그중 303개는 오탐 유도 함정)

| | 1층만 | 형태소 층 포함 |
| --- | --- | --- |
| 정밀도 | **1.000** | **1.000** |
| 재현율 | 0.851 | 0.869 |

범주는 두 차례에 걸쳐 확장했다 — 1차(되/돼, 안/않, 어휘, 의존명사, 조사·어미, 이/히)에
2차(외래어 표기법, 높임 표현, 겹말, 한자어 오용, 자소서 문어체, 구어체)를 더했다.
2차 확장 직후 기존 규칙은 새 함정 138개에서 오탐 0을 유지했다.

### 성능

| | |
| --- | --- |
| 1층 | 0.10 ms / 1,000자 (2만 자 1.9 ms) |
| 형태소 층 | 19 ms / 1,000자 |
| 형태소 층 초기화 | 70~101 ms (한 번만) |

### 기각한 것

- **점수 기반 오타 탐지(2층)** — 정밀도가 1.000에서 0.892로 떨어졌습니다.
  임계값을 만지는 대신 분포를 재서 정상 단어와 오타가 겹친다는 것을 확인하고 기각했습니다.
  [ADR 0006](docs/decisions/0006-rejected-score-based-typo-detection.md)에 측정값이 있습니다.

### 고친 것

- 형태소 분석이 길이에 초선형이었습니다. 문장 단위로 쪼개 선형으로 만들었습니다 (2만 자 14초 → 0.38초).
- `contenteditable`에서 커서 위치를 텍스트 노드 기준으로 읽어, 긴 글에서 늘 앞부분만 검사했습니다.
