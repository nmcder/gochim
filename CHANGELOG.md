# 변경 기록

이 파일은 사람이 읽으라고 있습니다. 무엇이 바뀌었는지보다 **왜 바뀌었는지**를 적습니다.

## 아직 배포하지 않음

### 어절 안쪽 띄어쓰기를 형태소 층이 맡는다

붙여 쓴 어절을 **품사로** 가릅니다. 짝 목록으로는 닫히지 않는 집합이었습니다.

    물이끓으면 · 기름을두르고        조사 + 용언
    남은재료는 · 힘든날에는          관형사형 + 자립명사
    할수있다 · 만날거야              의존명사
    많이내리기 · 아무리봐도          부사 + 용언
    학교끝나고 · 고장나면            명사 + 용언
    늦지않고 · 보고싶다 · 앉게됐다   어미 + 보조용언

한 낱말로 굳은 말은 분석기가 통째로 한 형태소로 내놓는다(`알아보/VV`)는 성질이 가드입니다.
1층에서 블록리스트로 막아야 했던 것이 저절로 걸러집니다.

갈래마다 규칙을 따로 두었더니 한 어절에서 두 진단이 겹쳐 엔진이 하나만 남겼습니다.
`잘못나온거`가 `잘못 나온거`로 반만 고쳐지던 자리라, 어절을 한 번 훑어 자를 곳을
모두 모은 뒤 **한 진단으로** 냅니다.

### 그 밖에 새로 잡는 것

- `시킬려고 → 시키려고`. ㄹ받침 꼴이 실제 용언인지 되물어 `칼을 갈려고`를 비켜 갑니다.
- `느낌이였다 → 느낌이었다`. 앞말이 일반명사일 때만 — 사람 이름 `민준이였다`는 그대로 둡니다.
- `주문한지 30분 → 주문한 지 30분`. 앞이 용언이고 뒤에 지나온 시간이 오는 자리만 봅니다.
- `어떻하냐고 → 어떡하냐고`, `아니였나 → 아니었나`, `다른데 가자 → 다른 데 가자`
- 단위 명사 확충 — `세컵`, `삼백그램`, `두큰술`

### 고친 것

- **모두 고치기가 한 번만 훑던 문제.** `먹을껄`은 띄어쓰기와 표기가 함께 틀려
  한 번으로는 `먹을 껄`에서 멈췄습니다. 이제 세 번까지 훑습니다.
- `약속시간`을 경고에서 오류로 올렸습니다. `좀더·둘다`는 단음절 단어가 이어질 때
  붙여 쓸 수 있다는 제46항이 걸쳐 있지만 `약속시간`에는 걸칠 조항이 없습니다.

### 한자어·표기 혼동, 높임 호응

없는 낱말은 사전에 바로 넣었습니다 — `치루고→치르고`, `가르켜→가르쳐`, `쉽상→십상`,
`제작년→재작년`, `조취를→조치를`, `알맞는→알맞은`, `무릅이→무릎이`,
`알아보실런지→알아보실는지`, `나갔구요→나갔고요`.

두 낱말이 다 실재하는 것은 **근거가 되는 말이 두 어절 안에 있을 때만** 고칩니다.
`카드로 결재→결제`, `회원증을 경신→갱신`, `최고 기록을 갱신→경신`, `무게를 늘이면→늘리면`,
`국물을 조리는→졸이는`, `밥을 앉혀→안쳐`, `면이 불기→붇기`, `일체 넣지 않았다→일절`.

높임은 두 방향을 다 봅니다.

- **빠진 높임** — `어머니께서 알려 줬는데 → 주셨는데`. 어간 위치를 알면 표 없이
  `-시-`를 끼울 수 있습니다. 어려운 것은 활용이 아니라 어디까지가 그 주어의 서술어인가입니다.
- **넘치는 높임** — `환불이 가능하십니다 → 가능합니다`. 조사가 붙어야만 발화합니다.
  `주문하신 아메리카노`의 `주문`은 주어가 아니라 서술어의 일부라서요.
- **어휘 높임** — `선생님께 물어봤을 → 여쭤봤을`, `선생님 생일 → 생신`, `어르신께 주었다 → 드렸다`

### 자동 고침이 타이핑을 앞지르던 문제

**기본값을 켜짐으로 바꿨습니다.** 켜져 있으면 밑줄도 카드도 뜨지 않고 알아서 고쳐집니다.

고칠 때 커서를 돌려놓지 않던 것이 가장 큰 사고였습니다. 앞 낱말이 고쳐지는 순간
커서가 그 자리로 끌려가서, 이어 치던 글자가 문장 가운데에 박혔습니다 —
`안녕하세요 참 않됬네요 화이팅`이 `안녕하세요 참 않 화이팅됬네요`가 되는 식입니다.

세 가지를 함께 손봤습니다.

- **커서를 되돌려 놓습니다.** 고친 길이 차이만큼 밀어 원래 자리에 놓습니다.
- **한글 조합 중에는 기다립니다.** 자모를 모으는 동안 DOM을 건드리면 조합이 깨져
  치던 글자가 통째로 날아갑니다.
- **0.7초를 기다립니다.** 검사는 0.3초 뒤에 돌지만 고치는 것은 더 뜸을 들입니다.
  빨리 치는 사람은 낱말 사이에서도 손이 멈추지 않습니다.

글자를 한 자라도 친 칸에서만 움직입니다. 남이 써 둔 글을 눌렀을 뿐인데 바뀌면 놀라니까요.
규정이 양쪽을 다 허용하는 표기(`좀더`·`둘다`)도 그대로 둡니다.

언제 고치는지도 다시 잡았습니다. **낱말이 끝나는 순간**(공백·문장부호) 그 앞을 바로 고치고,
지금 치고 있는 낱말은 **입력칸에서 손을 뗄 때** 고칩니다. 다 못 친 낱말은 거의 언제나
틀린 말처럼 보이기 때문입니다 — 정상 문장 900개를 한 글자씩 쳐 나가며 세어 보니
진행 중인 어절에 진단이 붙는 자리가 24가지 나왔습니다(`후들거`→`후들 거`, `안간`→`안 간`).

손을 뗄 때 고치는 길에서는 초점을 건드리지 않습니다. 되돌리기 스택을 지키는 방법이
초점을 요구하는데, 그걸 그대로 쓰면 다음 칸으로 넘어가려던 Tab이 되돌아옵니다.

`apps/extension/test/autofix.html`이 이 사고를 그대로 재현합니다.
커서 되돌리기를 빼면 곧바로 실패하는 것까지 확인했습니다.

설정 화면은 IndexedDB를 못 열어도 뜹니다. 예전에는 그 자리에서 멈춰
켜고 끄는 스위치까지 죽었습니다.

### 재는 법

- `npm run guard` 가 **형태소 층까지 함께** 잽니다. 확장이 분석기를 기본값으로 켜 두므로
  1층만 재면 사용자가 보는 결과를 재지 않는 것이 됩니다.
- 표본을 15편 464건으로 늘렸습니다. 사용자가 확장에 직접 넣어 보고 짚어 준 글 두 편이
  들어 있습니다. 짚어 준 것만 고치면 그 글에만 맞춘 규칙이 됩니다.
- 여러 갈래 재현율 **0.955** (1층만 0.786) · 과교정 0건
- 모두 고친 뒤 정답과 글자까지 같은 글 **5/15** (표본을 넣었을 때는 0/15)

## [0.1.0] — 2026-08-19

첫 동작 버전. 웹 데모와 크롬 확장이 모두 돌아갑니다.

### 엔진

- `@gochim/core` — 한국어 맞춤법·띄어쓰기 교정 엔진. 순수 함수 `check(text) → Diagnostic[]` 하나가 전부입니다.
  런타임 의존성 0개, gzip 18.5 kB. DOM도 크롬 API도 WASM도 모릅니다.
- 문자열 규칙 41개. 사전형 규칙은 항목이 수백 개여도 정규식 하나로 한 번만 훑습니다.
- 받침 계산으로 규정을 코드로 적습니다 — 두음법칙(`경쟁율→경쟁률`), `-이에요/-예요`, `-ㄹ게`.
- URL·이메일·코드·HTML 태그·파일 경로·짧은 인용은 검사에서 제외합니다.

### 형태소 층

- `@gochim/morph` — garu-ko 어댑터. 코어는 `Analyzer` 인터페이스만 알고 구현은 주입받습니다.
  덕분에 코어만 쓰면 18.5 kB로 끝나고, 정확도가 더 필요할 때만 1.6 MB를 얹습니다.
- 품사를 알자 1층이 예외 목록으로 겨우 막던 것들이 저절로 걸러졌습니다
  (`실수/NNG`, `날것/NNG`, `일리/NNG`, `밖에/JX` vs `밖/NNG+에/JKB`).
- 1층이 포기했던 갈래가 열렸습니다 — `먹을만큼만 → 먹을 만큼만`.

### 무시 사전

- `@gochim/store` — IndexedDB 기반. 같은 밑줄을 매번 다시 지우게 만들면 사용자는 도구를 끕니다.
- 저장이 막히면(사생활 보호 모드, Node) 메모리 모드로 조용히 내려갑니다.

### 웹 데모

- 붙여넣기 → 교정. 거울 하이라이트 레이어 + 어절 단위 문맥 카드 + 왜 틀렸는지 설명.
- 형태소 층은 토글로 켜고, 워커에서 돕니다.
- 결과 카드는 키보드로 다룰 수 있습니다.

### 크롬 확장 (MV3)

- **권한 목록에 네트워크가 없습니다.** `permissions`는 `storage` 하나뿐입니다.
- 호스트 DOM을 건드리지 않습니다 — contenteditable은 CSS Custom Highlight API,
  textarea는 겹쳐 놓은 거울 레이어. 편집기가 깨질 구조적 위험이 없습니다.
- 300ms 디바운스 + 4,000자 창으로 타이핑을 막지 않습니다.
- 교정 적용은 `execCommand('insertText')` — 되돌리기 스택을 유지하는 유일한 방법입니다.

### 정확도

골든 테스트셋(오류 문장 227 · 정상 문장 553, 그중 326개는 오탐 유도 함정)

| | 1층만 | 형태소 층 포함 |
| --- | --- | --- |
| 정밀도 | **1.000** | **1.000** |
| 재현율 | 0.939 | 0.952 |

범주는 두 차례에 걸쳐 확장했다 — 1차(되/돼, 안/않, 어휘, 의존명사, 조사·어미, 이/히)에
2차(외래어 표기법, 높임 표현, 겹말, 한자어 오용, 자소서 문어체, 구어체)를 더했다.
2차 확장 직후 기존 규칙은 새 함정 161개에서 오탐 0을 유지했다.

### 성능

| | |
| --- | --- |
| 1층 | 4,000자 1.07 ms (p95 1.59 ms) · 2만 자 8.2 ms |
| 형태소 층 | 19 ms / 1,000자 |
| 형태소 층 초기화 | 70~101 ms (한 번만) |

### 기각한 것

- **점수 기반 오타 탐지(2층)** — 정밀도가 1.000에서 0.892로 떨어졌습니다.
  임계값을 만지는 대신 분포를 재서 정상 단어와 오타가 겹친다는 것을 확인하고 기각했습니다.
  [ADR 0006](docs/decisions/0006-rejected-score-based-typo-detection.md)에 측정값이 있습니다.

### 고친 것

- 형태소 분석이 길이에 초선형이었습니다. 문장 단위로 쪼개 선형으로 만들었습니다 (2만 자 14초 → 0.38초).
- `contenteditable`에서 커서 위치를 텍스트 노드 기준으로 읽어, 긴 글에서 늘 앞부분만 검사했습니다.
