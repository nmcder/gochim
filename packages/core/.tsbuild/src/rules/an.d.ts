/**
 * 안 / 않.
 *
 * `안`은 부사(아니)라 홀로 서고 뒤 말과 띄어 쓴다.
 * `않-`은 용언 '아니하다'의 준말 어간이라 반드시 어미가 붙는다.
 * 그래서 `않되`처럼 어미 없이 다른 용언이 붙는 형태는 언제나 틀린다.
 */
export declare const anAnh: import("../types.js").Rule;
/**
 * 홀로 선 `않`.
 *
 * `않-`은 '아니하-'의 준말 **어간**이라 어미 없이는 설 수 없다.
 * 뒤에 공백이나 문장부호가 오면 무조건 부사 `안`을 써야 하는 자리다.
 */
export declare const anhAlone: import("../types.js").Rule;
export declare const bojoJiAnh: import("../types.js").Rule;
export declare const jiAnh: import("../types.js").Rule;
//# sourceMappingURL=an.d.ts.map