import { defineRule } from './define.js'
import type { Rule } from '../types.js'

/**
 * 부사가 뒷말에 붙어 버린 것, 그리고 목적어가 용언에 붙어 버린 것.
 *
 * 이 두 계열은 **일반 규칙으로 만들면 안 된다.** 부사도 목적어도 뒤엣말과 한 단어로
 * 굳은 경우가 수두룩해서, "부사 뒤는 띄운다" 같은 규칙을 세우면 멀쩡한 말이 갈라진다.
 *
 *   왜 → 왜곡·왜소·왜냐하면·왜인지·임진왜란
 *   더 → 더위·더욱·더구나·더미·더불어·더디다,  그리고 어미 `-더-`(없더라)
 *   좀 → 좀도둑·좀먹다
 *   밥먹 → 물먹다·욕먹다·겁먹다·맘먹다  (전부 사전 표제어)
 *
 * 그래서 여기서는 **한 단어가 될 여지가 없는 조합만 낱개로** 잡는다.
 * 재현율을 크게 포기하는 대신 오탐을 0으로 둔다.
 *
 * ## 여기 없는 것
 *
 * `좀더`는 오류가 **아니다.** 한글 맞춤법 제46항이 단음절 단어가 이어질 때 붙여 쓰기를
 * 허용하고, 그 조항의 예시에 `좀더 큰것`이 그대로 들어 있다.
 */

export const busaWae = defineRule({
  id: 'busa-wae',
  category: 'spacing',
  confidence: 0.93,
  // '왜' 뒤를 통째로 띄우면 '왜냐하면·왜인지'가 깨진다. 한 단어가 될 수 없는 '그러다' 계열만 잡는다.
  pattern: /(?<![가-힣])왜(그래|그러|그랬|그런|그럴|그리|이렇게)/g,
  resolve(ctx) {
    const tail = ctx.match[1] ?? ''
    return {
      suggestions: [`왜 ${tail}`],
      message: "부사 '왜'는 뒤 말과 띄어 씁니다.",
      explain:
        "'왜'는 부사, '그러다'는 동사로 각각 독립된 낱말입니다. 구어에서 '왜그래'를 붙여 적는 관행이 있으나 사전에 오른 한 단어가 아닙니다. '왜냐하면'이나 '왜인지'처럼 붙여 적는 말이 따로 있어, '왜' 뒤를 일괄로 띄우지 않고 한 단어가 될 수 없는 조합만 잡습니다.",
      refs: ['한글 맞춤법 제2항'],
    }
  },
  examples: [
    { wrong: '너는 도대체 왜그러냐고 따지듯 물었다.', right: '너는 도대체 왜 그러냐고 따지듯 물었다.' },
    { wrong: '갑자기 왜그래, 무슨 일 있어?', right: '갑자기 왜 그래, 무슨 일 있어?' },
    { wrong: '어제는 또 왜그랬는지 모르겠다.', right: '어제는 또 왜 그랬는지 모르겠다.' },
    { wrong: '사람들이 왜이렇게 많아졌을까.', right: '사람들이 왜 이렇게 많아졌을까.' },
    { wrong: '왜그럴까 한참 생각했다.', right: '왜 그럴까 한참 생각했다.' },
    { wrong: '왜그리 서두르는지 알 수 없었다.', right: '왜 그리 서두르는지 알 수 없었다.' },
    { wrong: '왜그런지 아무도 대답하지 않았다.', right: '왜 그런지 아무도 대답하지 않았다.' },
  ],
  counterExamples: [
    '왜냐하면 그날은 사정이 있었기 때문이다.',
    '사실을 왜곡하지 마라.',
    '왜소한 체구였지만 힘은 셌다.',
    '왜인지 자꾸 눈물이 났다.',
    '임진왜란 당시의 기록이 남아 있다.',
    '왜 그러는지 도무지 모르겠다.',
    '왜 그리 서두르니?',
    '왜 이렇게 늦었어?',
  ],
})

/** `꽤` 뒤에 이어 적는 것이 맞는 음절. `꽤나`는 한 단어이고 나머지는 부사에 붙는 보조사다. */
const KKWAE_TAIL_BLOCK = new Set(['나', '는', '도', '만'])

export const busaKkwae = defineRule({
  id: 'busa-kkwae',
  category: 'spacing',
  confidence: 0.92,
  // '꽤'는 이 계열에서 유일하게 함정이 없다. 사전에 '꽤'로 시작하는 표제어가 '꽤'와 '꽤나'뿐이다.
  pattern: /(?<![가-힣])꽤([가-힣])/g,
  resolve(ctx) {
    const next = ctx.match[1] ?? ''
    if (KKWAE_TAIL_BLOCK.has(next)) return null
    return {
      suggestions: [`꽤 ${next}`],
      message: "부사 '꽤'는 뒤 말과 띄어 씁니다.",
      explain:
        "'꽤'는 부사입니다. '꽤'로 시작하는 한 단어는 '꽤'와 '꽤나'뿐이라, 뒤에 다른 말이 붙어 있으면 띄어 써야 할 자리입니다.",
      refs: ['한글 맞춤법 제2항'],
    }
  },
  examples: [
    { wrong: '밖에 나가 보니 꽤많은 사람이 줄을 서 있었다.', right: '밖에 나가 보니 꽤 많은 사람이 줄을 서 있었다.' },
    { wrong: '생각보다 꽤오래 걸렸다.', right: '생각보다 꽤 오래 걸렸다.' },
    { wrong: '이 문제는 꽤어렵다.', right: '이 문제는 꽤 어렵다.' },
  ],
  counterExamples: ['꽤나 오래 걸린 작업이었다.', '값이 꽤 나가는 물건이다.', '기다린 보람이 꽤 있었다.', '꽤 많은 사람이 모였다.'],
})

export const busaDeoIsang = defineRule({
  id: 'busa-deo-isang',
  category: 'spacing',
  confidence: 0.93,
  // '더'는 함정이 너무 많아(더위·더욱·더구나·더불어, 어미 '-더-') 통째로 버리고 이 한 덩어리만 남겼다.
  pattern: /(?<![가-힣])더이상/g,
  resolve() {
    return {
      suggestions: ['더 이상'],
      message: "부사 '더'는 뒤 말과 띄어 씁니다.",
      explain:
        "'더'는 부사, '이상(以上)'은 명사로 서로 다른 낱말입니다. '더이상'은 사전에 없습니다. 단음절끼리 이어질 때 붙여 쓸 수 있다는 제46항은 '이상'이 두 음절이라 적용되지 않습니다.",
      refs: ['한글 맞춤법 제2항', '한글 맞춤법 제46항'],
    }
  },
  examples: [
    { wrong: '더이상 참을 수 없어서 자리를 떴다.', right: '더 이상 참을 수 없어서 자리를 떴다.' },
    { wrong: '나는 더이상 그 이야기를 꺼내지 않았다.', right: '나는 더 이상 그 이야기를 꺼내지 않았다.' },
    { wrong: '더이상은 못 기다리겠다.', right: '더 이상은 못 기다리겠다.' },
  ],
  counterExamples: [
    '더 이상 미룰 수 없다.',
    '그는 더욱더 열심히 했다.',
    '올여름은 더위가 유난히 심했다.',
    '더구나 비까지 내렸다.',
    '더욱 신중하게 판단했다.',
    '이상적인 방향으로 나아갔다.',
    '기대 이상으로 잘해 냈다.',
  ],
})

/** 표준국어대사전에 한 단어로 오른 `목적어+용언`. 정규식이 넓어지더라도 여기서 한 번 더 막는다. */
const SDICT_COMPOUND = new Set(['물먹', '욕먹', '겁먹', '맘먹', '애먹', '귀먹', '잠자', '꿈꾸', '힘쓰', '맛보', '본받'])

/**
 * 사전에 표제어가 **없음을 확인한** 조합만 여기 둔다.
 *
 * `물먹다`는 표제어인데 `물마시다`는 아니다. 이 비대칭이 정확히 위험 지점이라
 * 정규식에서 `물`은 `마시-`하고만 짝짓게 했다.
 */
const DICT_FORM: Record<string, string> = {
  밥: '밥먹다',
  옷: '옷입다',
  물: '물마시다',
  술: '술마시다',
  담배: '담배피우다',
}

export const mokjeogeoSpacing = defineRule({
  id: 'mokjeogeo-spacing',
  category: 'spacing',
  confidence: 0.93,
  pattern:
    /(?<![가-힣])(밥)(먹)(?=[고다더던든었어여은을음으는지자잖기게겠네니나면며습읍])|(?<![가-힣])(옷)(입)(?=[고다더던든었어여은을음으는지자잖기게겠네니나면며습읍])|(?<![가-힣])(물|술)(마시(?=[고다더던든는지자잖기게겠네니나면며러시죠])|마셔|마셨|마실|마신|마심)|(?<![가-힣])(담배)(피우(?=[고다더던든는지자잖기게겠네니나면며러시죠])|피워|피웠|피울|피운|피움)/g,
  resolve(ctx) {
    const obj = ctx.match[1] ?? ctx.match[3] ?? ctx.match[5] ?? ctx.match[7] ?? ''
    const verb = ctx.match[2] ?? ctx.match[4] ?? ctx.match[6] ?? ctx.match[8] ?? ''
    if (!obj || !verb) return null
    // 어간 첫 음절로 합성어 여부를 본다 — '물먹-'처럼 사전에 오른 말이면 가르지 않는다.
    if (SDICT_COMPOUND.has(obj + verb.slice(0, 1))) return null
    const dict = DICT_FORM[obj]
    if (!dict) return null
    return {
      suggestions: [`${obj} ${verb}`],
      subId: obj + verb,
      message: '목적어와 용언은 띄어 씁니다.',
      explain: `표준국어대사전에 '${dict}'라는 한 단어는 없습니다. 조사 '을/를'이 생략된 목적어일 뿐이므로 뒤의 용언과 띄어 씁니다. '잠자다·꿈꾸다·힘쓰다·맛보다·본받다·물먹다'처럼 사전에 오른 말은 반대로 붙여 씁니다.`,
      refs: ['한글 맞춤법 제2항'],
    }
  },
  examples: [
    { wrong: '내일 같이 밥먹으러 가자고 했다.', right: '내일 같이 밥 먹으러 가자고 했다.' },
    { wrong: '이따가 밥먹고 얘기하자.', right: '이따가 밥 먹고 얘기하자.' },
    { wrong: '급하게 옷입고 밖으로 나왔다.', right: '급하게 옷 입고 밖으로 나왔다.' },
    { wrong: '목이 말라서 물마시러 나갔다.', right: '목이 말라서 물 마시러 나갔다.' },
    { wrong: '어제 친구들이랑 술마시고 늦게 들어왔다.', right: '어제 친구들이랑 술 마시고 늦게 들어왔다.' },
    { wrong: '여기서 담배피우면 안 됩니다.', right: '여기서 담배 피우면 안 됩니다.' },
  ],
  counterExamples: [
    '내일 같이 밥 먹으러 가자.',
    '요즘 입맛이 없어서 밥을 잘 안 먹어.',
    '어제 술 마시고 늦게 들어왔다.',
    '물 마시러 잠깐 나갔다 올게.',
    '여기서 담배 피우시면 안 돼요.',
    '어젯밤엔 잠자느라 전화를 못 받았다.',
    '이상한 꿈꾸고 새벽에 깼다.',
    '다들 힘쓰는 일에는 손사래를 쳤다.',
    '이번에 처음 맛보는 음식이었다.',
    '선배의 성실함을 본받고 싶다.',
    '나도 마음먹고 시작한 일이다.',
    '시험에서 보기 좋게 물먹었다.',
    '괜히 욕먹을 짓은 하지 말자.',
    '겁먹지 말고 한번 해 봐.',
    '동생이 애먹는 걸 보고 도와줬다.',
    '비빔밥 먹으러 갈까?',
    '겉옷 입고 나가.',
    '찬물 마시면 이가 시리다.',
    '전자담배 피우는 사람도 늘었다.',
    '밥값은 내가 낼게.',
    '밥맛이 뚝 떨어졌다.',
  ],
})

/**
 * 대명사 `뭐` + 동사 `하다`.
 *
 * 사전에는 형용사 `뭐하다`(=`뭣하다`, 언짢거나 거북하다)가 따로 올라 있다.
 * 그래서 `뭐하-`를 통째로 가르면 "말하기가 좀 뭐하다"가 깨진다.
 *
 * 두 쓰임을 가르는 것은 **앞말**이다. 형용사 쪽은 앞에 정도 부사(좀·조금·영)나
 * `-기` 명사형(말하기·거절하기·가기)이 온다. 무엇을 하느냐고 묻는 쪽은 그렇지 않다.
 *
 *   내일 뭐하냐고 물어봤다     → 뭐 하냐고   (대명사 + 동사)
 *   혼자 가기는 뭐하니까       → 그대로      (형용사)
 *
 * 뒤 어미도 의문·관형 쪽으로 좁혔다. `뭐하고`는 양쪽에 다 쓰여서 뺐다.
 */
const MWO_ADJECTIVE_BEFORE = /(?:좀|조금|영|참|다소|약간|[가-힣]+기(?:가|도|는|를|엔|에|나)?)\s*$/

export const mwoHada = defineRule({
  id: 'mwo-hada',
  category: 'spacing',
  confidence: 0.9,
  pattern: /(?<![가-힣])뭐하(냐|니|는|세|실|십|자)|(?<![가-힣])뭐할(?=[까래])/g,
  resolve(ctx) {
    if (MWO_ADJECTIVE_BEFORE.test(ctx.text.slice(0, ctx.index))) return null
    return {
      suggestions: [`뭐 ${ctx.match[0].slice(1)}`],
      message: "'뭐'는 대명사라 뒤의 용언과 띄어 씁니다.",
      explain:
        "무엇을 하느냐고 묻는 '뭐 하다'는 대명사 '뭐'와 동사 '하다'가 이어진 두 낱말입니다. 사전에 오른 '뭐하다'는 '언짢다·거북하다'라는 뜻의 형용사로, '말하기가 좀 뭐하다'처럼 앞에 정도 부사나 '-기' 명사형이 옵니다.",
      refs: ['한글 맞춤법 제2항'],
    }
  },
  examples: [
    { wrong: '친구가 갑자기 내일 뭐하냐고 물어봤다.', right: '친구가 갑자기 내일 뭐 하냐고 물어봤다.' },
    { wrong: '지금 뭐하니?', right: '지금 뭐 하니?' },
    { wrong: '뭐하는 사람인지 모르겠다.', right: '뭐 하는 사람인지 모르겠다.' },
    { wrong: '주말에 뭐할까 고민했다.', right: '주말에 뭐 할까 고민했다.' },
    { wrong: '지금 뭐하세요?', right: '지금 뭐 하세요?' },
  ],
  counterExamples: [
    '내가 말하기는 좀 뭐하다.',
    '그렇게 말하기도 뭐하고 그냥 넘어갔다.',
    '혼자 가기는 뭐하니까 같이 가자.',
    '거절하기가 뭐해서 그냥 갔다.',
    '좀 뭐한 상황이라 조용히 있었다.',
    '지금 뭐 하니?',
    '내일 뭐 할지 정했어?',
  ],
})

export const busaRules: Rule[] = [busaWae, busaKkwae, busaDeoIsang, mokjeogeoSpacing, mwoHada]
